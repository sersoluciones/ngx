import { ClickPointerDirective } from './click-pointer.directive';

describe('ClickPointerDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickPointerDirective();
    expect(directive).toBeTruthy();
  });
});
